name: hawtio-mapping

on:
  # Manual trigger (on-demand)
  workflow_dispatch:
  schedule:
    # Runs at 06:20 UTC on the 1st and 15th of every month
    - cron: '20 6 1,15 * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  call-mapping-workflow:
    uses: abeyta-labs/saa-workflows/.github/workflows/create-mapping.yml@main
    with:
      mapping-name: hawtio
      repo-url: "https://github.com/hawtio/hawtio"
      coordinates: "io.hawt:hawtio-springboot"
    secrets: inherit