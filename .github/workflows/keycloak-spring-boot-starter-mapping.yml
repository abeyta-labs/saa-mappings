name: keycloak-spring-boot-starter-mapping

on:
  # Manual trigger (on-demand)
  workflow_dispatch:
  repository_dispatch:
    types: [keycloak-spring-boot-starter]
  schedule:
    # Runs based on the configured schedule
    - cron: '0 9 1,15 * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  call-mapping-workflow:
    uses: abeyta-labs/saa-workflows/.github/workflows/build-mapping.yml@main
    with:
      mapping-name: keycloak-spring-boot-starter
      repo-url: https://github.com/keycloak/keycloak
      coordinates: org.keycloak:keycloak-spring-boot-starter
    secrets: inherit
