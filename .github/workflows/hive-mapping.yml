name: hive-mapping

on:
  # Manual trigger (on-demand)
  workflow_dispatch:
  schedule:
    # Runs at 06:40 UTC on the 1st and 15th of every month
    - cron: '40 6 1,15 * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  call-mapping-workflow:
    uses: abeyta-labs/saa-workflows/.github/workflows/create-mapping.yml@main
    with:
      mapping-name: hive
      repo-url: "https://github.com/apache/hive"
    secrets: inherit